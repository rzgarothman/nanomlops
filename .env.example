# ==========================================
# MLOps Infrastructure Environment Variables
# ==========================================

# --- 基础配置 ---
DOMAIN_NAME=mlops.local
TIMEZONE=Asia/Shanghai
# 数据存储根目录
DATA_ROOT=./data
# 服务重启策略 (默认 unless-stopped, 生产环境可选 always)
RESTART_POLICY=unless-stopped

# --- 版本控制 (尽量使用较新的稳定版) ---
# [Docker Image Tags] 用于拉取服务镜像
POSTGRES_TAG=16-alpine
REDIS_TAG=7-alpine
MINIO_TAG=RELEASE.2025-09-07T16-13-09Z
GITEA_TAG=1.22
GITEA_ACT_RUNNER_TAG=latest
MLFLOW_TAG=v3.5.1
PREFECT_TAG=3-python3.11
LABEL_STUDIO_TAG=1.22.0
EVIDENTLY_TAG=0.7.18
NGINX_TAG=alpine
PROMETHEUS_TAG=v3.5.0
GRAFANA_TAG=11.6

# [Pip Package Versions] 用于 Workspace 内部安装 (需符合 pip 版本规范)
MLFLOW_PIP_VERSION=3.5.1
PREFECT_PIP_VERSION=3.5.0
EVIDENTLY_PIP_VERSION=0.7.18

# --- 端口映射 (宿主机端口) ---
PORT_NGINX=80
PORT_MINIO_API=9000
PORT_MINIO_CONSOLE=9001
PORT_GITEA_SSH=2222
PORT_GITEA_HTTP=3000
PORT_MLFLOW=5000
PORT_PREFECT=4200
PORT_LABEL_STUDIO=8080
PORT_EVIDENTLY=8000

PORT_JUPYTER=8888
PORT_VSCODE=8081
PORT_PROMETHEUS=9090
PORT_GRAFANA=3001
PORT_BENTO_SERVING=3002
PORT_STREAMLIT=8501
PORT_NAV=80

# --- 凭证与安全 (生产环境请修改强密码) ---
# Postgres
POSTGRES_USER=admin
POSTGRES_PASSWORD=secure_pg_password
# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
# Gitea
GITEA_DB_PASSWORD=gitea_db_password
# MLflow
MLFLOW_DB_PASSWORD=mlflow_db_password
# Prefect
PREFECT_DB_PASSWORD=prefect_db_password
# Label Studio
LABEL_STUDIO_DB_PASSWORD=ls_db_password

# --- Workspace 默认鉴权信息 (可被用户 .env 覆盖) ---
PASSWORD=workspace_password
JUPYTER_TOKEN=workspace_token